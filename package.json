{
  "name": "@zhaolibin/smart-browser-mcp",
  "version": "1.0.0",
  "description": "Browser automation and API testing MCP server built on Playwright + CDP. Numbered element refs and Set-of-Mark screenshots let AI agents target the right node; flow record/replay, element memory, captcha handoff and an HTTP API test suite.",
  "main": "./dist/index.js",
  "type": "module",
  "bin": {
    "smart-browser-mcp": "./dist/index.js",
    "claude-browser-mcp": "./dist/index.js",
    "browser-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --format=esm --outfile=dist/index.js --external:@modelcontextprotocol/sdk --external:playwright --external:zod",
    "build:minify": "esbuild src/index.js --bundle --platform=node --format=esm --minify --outfile=dist/index.js --external:@modelcontextprotocol/sdk --external:playwright --external:zod",
    "prepublishOnly": "npm run build",
    "start": "node dist/index.js",
    "dev": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "mcp-server",
    "model-context-protocol",
    "claude",
    "claude-code",
    "browser",
    "browser-automation",
    "playwright",
    "automation",
    "cdp",
    "e2e",
    "api-testing",
    "ai-agents"
  ],
  "author": "zhaolibin001",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zhaolibins001-svg/smart-browser.git"
  },
  "homepage": "https://gitee.com/zhaolibin001/smart-browser",
  "bugs": {
    "url": "https://github.com/zhaolibins001-svg/smart-browser/issues"
  },
  "license": "ISC",
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "playwright": "^1.61.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
